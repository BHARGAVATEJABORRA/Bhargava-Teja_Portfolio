// Dock is now rendered inside HeroSection as HeroDock (hero-section.tsx).
// This file is kept as an empty shell to avoid import errors if referenced elsewhere.
export function QuickAccessDock() {
  return null;
}
