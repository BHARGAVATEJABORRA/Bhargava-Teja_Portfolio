# Plan (§2.b.2) — checkbox tracker

- [x] §1a inventory → NOTES_diagnosis.md
- [ ] Harness: Playwright under `frontend/tests/scroll/`, `npm run verify` = lint → build → scroll harness (desktop 1440 + mobile 390)
- [ ] §3.1 footer skid: remove `useSpring` second clock; layers = pure function of `scrollYProgress`
- [ ] §3.2 meteor blob: JPEG sprite → gradient trail fading to transparent
- [ ] §3.3 aurora: hoist to app root, persistent ambient, scroll-modulated intensity, no IO freeze
- [ ] §4 articles deck: scroll-progress scale/opacity falloff + reduced-motion fallback
- [ ] §5 AI dock: verify hover spin / click open / no overlap / mobile
- [ ] Final: `npm run verify` green both viewports; NOTES_summary.md; changelog; atomic commits

## Metrics log

(appended after each verify run)
